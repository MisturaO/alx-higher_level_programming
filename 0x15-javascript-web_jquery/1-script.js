/* global $ */
// JavaScript script that updates the text color of the <header>
// element to red (#FF0000) using jQuery API.

// // Stores the element's tag to modify, inside the var 'myHeader'
const myHeader = $('header');

// Upadtes the <header> tag's text to red
myHeader.css('color', '#FF0000');
